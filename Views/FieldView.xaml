<UserControl x:Class="PatientReport.Views.FieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PatientReport.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <local:FieldSelectionView DockPanel.Dock="Left"/>
            <local:FieldDetailsView DockPanel.Dock="Right" DataContext="{Binding SelectedField}"/>
        </DockPanel>
    </Grid>
</UserControl>
